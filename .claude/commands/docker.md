Help me with Docker. Explain Docker in simple terms first (Docker is like a lunchbox that contains your app and everything it needs to run anywhere).

I might need help with:
- Writing a Dockerfile
- Setting up docker-compose
- Running containers
- Debugging container issues
- Deploying with Docker

Always explain what each Docker command does before running it.

Docker task: $ARGUMENTS
