Help me with my database. I might need help with:
- Writing SQL queries (explain each part in plain English)
- Designing a database schema (tables and relationships)
- Optimizing slow queries
- Migrations (changing the database structure safely)
- Connecting my app to the database

Always explain database concepts using real-world analogies (a database is like a spreadsheet, a table is like a sheet, etc.)

Database task: $ARGUMENTS
